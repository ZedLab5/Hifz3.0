# Priority Rules
- **Absolute Priority: App Performance.** Before implementing any feature that may introduce CPU/memory overhead or background load (e.g., extensive calendar calculations, background tasks), propose it and obtain explicit user confirmation first.
